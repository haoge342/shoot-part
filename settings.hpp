#ifndef SETTINGS_HPP
#define SETTINGS_HPP

struct Settings{
    int windowWidth;
    int windowHeight;
    float globalAnimationSpeed;
    float dampingFactor;
    float groundFriction;
    float gravity;
    float velocityThreshold;
    float restitution;
}

#endif